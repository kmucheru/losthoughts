<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "LosThoughts" %></title>
    
    <%= stylesheet_link_tag    "application" %>

    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <%= render partial: 'layouts/brandbar' %>
    </header>
    <%= yield %>
    <footer>
      <%= render partial: 'layouts/footer' %>
    </footer>
    <%= javascript_include_tag "application" %>
  </body>
</html>
